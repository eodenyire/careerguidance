<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/enlighteducate"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/enlighteducate"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".First"></activity>
        <activity android:name=".HealthScheme"></activity>
        <activity android:name=".Mpc"></activity>
        <activity android:name=".AfterInter"></activity>
        <activity android:name=".Engineering"></activity>

    </application>

</manifest>